# The repo's conda environment. Create with:  conda env create -f environment.yml
# Activate with:                              conda activate papers
name: papers
channels:
  - conda-forge
dependencies:
  - python=3.12
  - numpy
  - scipy
  - matplotlib
  - pandas
  - scikit-learn
  - jupyter
  - ipykernel
  - pip
